FROM nginx:alpine

# Устанавливаем рабочую директорию /app внутри контейнера
WORKDIR /app

# Копируем статические файлы вашего фронтенд-приложения в директорию /app
COPY . .

# Указываем порт, на котором будет работать веб-сервер Nginx (обычно порт 80)
EXPOSE 80

# Команда для запуска веб-сервера Nginx в контейнере
CMD ["nginx", "-g", "daemon off;"]

